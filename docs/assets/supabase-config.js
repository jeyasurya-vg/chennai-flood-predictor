// Fill these in after creating your free Supabase project:
// Project Settings -> API -> Project URL / anon public key.
//
// The anon key is DESIGNED to be public and safe to commit — Supabase's
// security model puts protection in the Row Level Security policies
// (see supabase/schema.sql), not in hiding this key. Do not put a
// service_role key here; that one must stay secret and server-side only.

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
